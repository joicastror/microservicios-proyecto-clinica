services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql_clinica
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: autenticacion
    ports:
      - "3306:3306"
    restart: always