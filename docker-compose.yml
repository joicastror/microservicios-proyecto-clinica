version: '3.8'

services:
  db-clinica:
    image: mysql:8.0
    container_name: db-clinica
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD:
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./init:/docker-entrypoint-initdb.d

volumes:
  db_data: